#!/bin/bash
# Railway start script for Voice Reminder Backend API

echo "Starting Voice Reminder Backend API..."
echo "Node version: $(node --version)"
echo "NPM version: $(npm --version)"

# Start the application
exec npm start
